<p><%= @task.title %></p>
<p><%= @task.details %></p>
<p><%= link_to "Edit task", edit_path(@task) %></p>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
